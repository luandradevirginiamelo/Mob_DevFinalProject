package com.example.finalproject_mobdev.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

// Definição padrão de Shapes para o MaterialTheme
val Shapes = Shapes(
    small = RoundedCornerShape(8.dp),    // Formas pequenas com cantos arredondados de 8dp
    medium = RoundedCornerShape(16.dp),  // Formas médias com cantos arredondados de 16dp
    large = RoundedCornerShape(24.dp)    // Formas grandes com cantos arredondados de 24dp
)


